package main

import "github.com/edrumm/markup"

func main() {
	/*
		Create new html page
		(*HTML)
	*/
	index := markup.HtmlPage("index.html", "en", "A Markup Page")

	// Append elements
	index.Body(markup.Tag("h1", "Auto Generated Page"))
	index.Body(markup.SingleTag("hr"))

	// Paragraph with nested tags
	index.Body(markup.Tag("p",
		"An HTML page generated by Markup Go package",
		markup.Tag("strong", "Some subtext").Html(),
		markup.Hyperlink("https://github.com/edrumm/markup", "GitHub link").Html(),
		"and back to normal...",
	))

	index.Body(markup.Hyperlink("https://pkg.go.dev/github.com/edrumm/markup", "Markup on pkg.go.dev"))
	index.Body(markup.SingleTag("br"))
	index.Body(markup.SingleTag("br"))
	index.Body(markup.ImgWithSize("1024px-Go_Logo_Blue.svg.png", "Go logo", 341, 128))

	/*
		Div tag
		(*HtmlDiv)
	*/
	div := markup.Div()
	div.Append(markup.Tag("h2", "Div Header"))
	div.Append(markup.Tag("p", "Some div text"))

	index.Body(div)

	/*
		Create new stylesheet
		(*Stylesheet)
	*/
	css := markup.NewStylesheet("style.css")

	// Render page to html file
	index.Render()

	// Render stylesheet to css file
	css.Render()
}
